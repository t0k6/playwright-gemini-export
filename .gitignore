# Node / deps
node_modules/

# Tool output (exported context)
.ai-context/

# Cursor internal
.cursor/

# Git linked worktrees (local comparative review)
.worktrees/

# Local user config (commit only the example)
.gemini-export.json
