# Node / deps
node_modules/

# Tool output (exported context)
.ai-context/

# Cursor internal
.cursor/

# Local user config (commit only the example)
/.gemini-export.json

# playwright project
playwright/

# Trial / integration debug NDJSON (GEMINI_EXPORT_TRIAL_DEBUG=1)
debug-trial-export-cli.ndjson
debug-trial-export-cli.*.ndjson
